[project]
name = "llms-txt-generator"
version = "0.1.0"
description = "Generate localized llms.txt documentation indexes."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "PyYAML==6.0.2",
]
